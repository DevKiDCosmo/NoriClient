# Registration

Program check if any registration is already done. If not this can proceed.

Install validation -> There is a key this will be for further connection.
Id with client uuid are taken and created a request to server. Server creates ws and they connect.
Information exchange.